import final_project_part1 as fp1

# G = Directed weighted graph 
# s = Source node 
# d = Destination node 
# h = Heuristic function. This is a dictionary which takes in a node (int)
#     and returns a float. 
def a_star(G, s, d, h): 
    return  

# return ({predecessor dictionary}, shortest path the algorithm determines
# from s to d)
